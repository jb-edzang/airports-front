/*import NextLink from "next/Link"

const Link = (props) => {
  const { href } = props

  return (
    <NextLink href={href}>
      <Link {..."otherProps"} />
    </NextLink>
  )
}

export default Link*/
